import styled from '@emotion/styled';

const Background = styled.div`
  position: fixed;
  inset: 0;
  background: url('/images/index/bg.jpg') no-repeat center 20% / cover;
`;

export default Background;
